# Shared by both the desktop app and the Streamlit app
requests
beautifulsoup4
anthropic

# Streamlit app only
streamlit
pandas

# Desktop app shell only (not needed to run `streamlit run streamlit_app.py`)
pywebview
pyinstaller
